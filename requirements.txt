numpy>=1.24
scipy>=1.10
pandas>=2.0
scikit-learn>=1.3
soundfile>=0.12
matplotlib>=3.7
tqdm>=4.65
huggingface_hub>=0.23
pyarrow>=14.0          # optional: parquet caching of clip measurements

# --- optional, only needed for `--backend birdnet` -------------------------
# birdnetlib>=0.17
# tensorflow>=2.15
# --- optional, only needed for the ARIA reference detector -----------------
# aria-inference
